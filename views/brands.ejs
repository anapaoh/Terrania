<section class="page-header">
    <div class="container">
        <h1>Marcas Colaboradoras</h1>
        <p>Conoce a nuestros socios productores y artesanos de Hidalgo</p>
    </div>
</section>

<section class="brands-intro">
    <div class="container">
        <div class="intro-content">
            <h2>Nuestras Alianzas Estratégicas</h2>
            <p>
                En Terranía creemos que la colaboración es clave para el éxito. 
                Trabajamos de la mano con productores, artesanos, cooperativas y 
                pequeñas empresas de todo el estado de Hidalgo que comparten 
                nuestros valores de calidad, sustentabilidad y comercio justo.
            </p>
        </div>
    </div>
</section>

{{#if brands}}
<section class="brands-showcase">
    <div class="container">
        <div class="brands-grid">
            {{#each brands}}
            <div class="brand-card">
                <div class="brand-header">
                    {{#if this.logo_url}}
                    <img src="{{this.logo_url}}" alt="{{this.name}}" class="brand-logo">
                    {{else}}
                    <div class="brand-placeholder">
                        <i class="fas fa-store"></i>
                    </div>
                    {{/if}}
                    
                    <h3 class="brand-name">{{this.name}}</h3>
                </div>
                
                <div class="brand-body">
                    {{#if this.description}}
                    <p class="brand-description">{{this.description}}</p>
                    {{/if}}
                    
                    <div class="brand-info">
                        {{#if this.location}}
                        <div class="info-item">
                            <i class="fas fa-map-marker-alt"></i>
                            <span>{{this.location}}</span>
                        </div>
                        {{/if}}
                        
                        {{#if this.established_date}}
                        <div class="info-item">
                            <i class="fas fa-calendar-alt"></i>
                            <span>Desde {{this.established_date}}</span>
                        </div>
                        {{/if}}
                    </div>
                </div>
                
                <div class="brand-footer">
                    {{#if this.website}}
                    <a href="{{this.website}}" target="_blank" class="brand-link">
                        <i class="fas fa-external-link-alt"></i> Sitio Web
                    </a>
                    {{/if}}
                    
                    {{#if this.contact_email}}
                    <a href="mailto:{{this.contact_email}}" class="brand-link">
                        <i class="fas fa-envelope"></i> Contactar
                    </a>
                    {{/if}}
                </div>
            </div>
            {{/each}}
        </div>
    </div>
</section>
{{else}}
<section class="no-brands">
    <div class="container">
        <div class="no-brands-content">
            <i class="fas fa-handshake"></i>
            <h3>Próximamente</h3>
            <p>Estamos trabajando para incorporar más marcas colaboradoras. ¡Mantente atento!</p>
        </div>
    </div>
</section>
{{/if}}

<section class="join-us">
    <div class="container">
        <div class="join-content">
            <h2>¿Quieres ser parte de Terranía?</h2>
            <p>
                Si eres productor, artesano o tienes una empresa comprometida con 
                la calidad y la sustentabilidad en Hidalgo, nos encantaría conocerte.
            </p>
            
            <div class="requirements">
                <h3>Requisitos para colaborar:</h3>
                <div class="requirements-grid">
                    <div class="requirement">
                        <i class="fas fa-check-circle"></i>
                        <span>Productos naturales o artesanales</span>
                    </div>
                    <div class="requirement">
                        <i class="fas fa-check-circle"></i>
                        <span>Origen en el estado de Hidalgo</span>
                    </div>
                    <div class="requirement">
                        <i class="fas fa-check-circle"></i>
                        <span>Compromiso con la calidad</span>
                    </div>
                    <div class="requirement">
                        <i class="fas fa-check-circle"></i>
                        <span>Prácticas sustentables</span>
                    </div>
                    <div class="requirement">
                        <i class="fas fa-check-circle"></i>
                        <span>Valores de comercio justo</span>
                    </div>
                    <div class="requirement">
                        <i class="fas fa-check-circle"></i>
                        <span>Preservación de tradiciones</span>
                    </div>
                </div>
            </div>
            
            <div class="join-cta">
                <a href="mailto:colaboraciones@terrania.com" class="btn btn-primary">
                    <i class="fas fa-envelope"></i> Contactanos
                </a>
            </div>
        </div>
    </div>
</section>